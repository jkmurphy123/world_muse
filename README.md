# world_muse
A wizard driven app to guide story creation

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e '.[test]'
```

## Run

cd /home/ubuntu/projects/world_muse/src
python -m world_muse.main

